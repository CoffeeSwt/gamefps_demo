import { MainEngine } from "./MainEngine";
import { BasicObject } from "./BasicObject";
import { BasicLight } from "./BasicLight";

export default { MainEngine, BasicObject, BasicLight };
export { BasicLight };
export { MainEngine, BasicObject };
